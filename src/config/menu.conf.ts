import {
  Home as HomeIcon,
  User as UserIcon,
} from '@vicons/carbon'
import { CloudUploadOutlined as UploadIcon } from '@vicons/antd'

const iconMap = {
  home: HomeIcon,
  user: UserIcon,
  upload: UploadIcon,
}

export const APP_MENU = {
  iconMap,
}
