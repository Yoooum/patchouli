import {
  Home as HomeIcon,
  User as UserIcon,
} from '@vicons/carbon'
import { CloudUploadOutlined as UploadIcon } from '@vicons/antd'
import { ExtensionPuzzleOutline as extensionIcon } from '@vicons/ionicons5'

const iconMap = {
  home: HomeIcon,
  user: UserIcon,
  upload: UploadIcon,
  extension: extensionIcon,
}

export const APP_MENU = {
  iconMap,
}
